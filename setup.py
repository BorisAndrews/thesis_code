from setuptools import setup, find_packages

setup(
    name='avfet_modules',
    version='0.2',
    description="Modules for Boris's thesis",
    long_description=open('README.txt').read(),
    author='Boris D. Andrews',
    packages=find_packages(),
    install_requires=[],
)
